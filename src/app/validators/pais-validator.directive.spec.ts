import { PaisValidatorDirective } from './pais-validator.directive';

describe('PaisValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PaisValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
